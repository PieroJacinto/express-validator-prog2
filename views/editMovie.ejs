<!DOCTYPE html>
<html>

<head>
    <title>UdeSA Movies</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>

<body>
    <div class="container mt-5">
        <h1 class="text-center mb-4">UdeSA Movies</h1>
        <p class="text-center">Welcome to UdeSA Movies</p>
        <div class="d-flex justify-content-center mb-3">
            <a href="/movies/new" class="btn btn-primary mx-2">Estrenos</a>
            <a href="/movies/recomended" class="btn btn-primary mx-2">Recomendadas</a>
        </div>

        <h2 class="mb-4">Nueva Película</h2>
        <form action="/movies/update/<%= movie.id %>" method="POST">
            <div class="mb-3">
                <label for="title" class="form-label">Título</label>
                <input type="text" class="form-control" id="title" name="title" value="<%= movie.title %>" placeholder="Título">
            </div>
            <div class="mb-3">
                <label for="rating" class="form-label">Rating</label>
                <input type="number" class="form-control" id="rating" name="rating" value="<%= movie.rating %>" placeholder="Rating">
            </div>
            <div class="mb-3">
                <label for="awards" class="form-label">Premios</label>
                <input type="number" class="form-control" id="awards" name="awards" value="<%= movie.awards %>" placeholder="Premios">
            </div>
            <div class="mb-3">
                <label for="release_date" class="form-label">Fecha de Estreno</label>
                <input type="date" class="form-control" id="release_date" name="release_date" value="<%= movie.release_date %>" placeholder="Fecha de Estreno">
            </div>
            <div class="mb-3">
                <label for="length" class="form-label">Duración</label>
                <input type="number" class="form-control" id="length" name="length" value="<%= movie.length %>" placeholder="Duración (minutos)">
            </div>
            <div class="mb-4">
                <label for="genre_id" class="form-label">Género</label>
                <select class="form-select" id="genre_id" name="genre_id">
                    <% for(let i = 0; i < genres.length; i++) { %>
                        <option value="<%= genres[i].id %>" <%= movie.genre_id === genres[i].id ? 'selected' : '' %>> <%= genres[i].name %> </option>
                    <% } %>
                </select>
            </div>
            <button type="submit" class="btn btn-success">Guardar</button>
        </form>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>

</html>
