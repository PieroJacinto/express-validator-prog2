<!DOCTYPE html>
<html>

<head>
    <title>UdeSA Movies</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>

<body>
    <div class="container mt-5">
        <h1 class="text-center mb-4">UdeSA Movies</h1>
        <p class="text-center">Datos de la película <strong><%= movie.title %></strong></p>
        <div class="d-flex justify-content-center mb-3">
            <a href="/" class="btn btn-primary mx-2">Home</a>
        </div>

        <div class="card">
            <div class="card-body">
                <h5 class="card-title">Información de la Película</h5>
                <p class="card-text"><strong>Nombre:</strong> <%= movie.title %></p>
                <p class="card-text"><strong>Rating:</strong> <%= movie.rating %></p>
                <p class="card-text"><strong>Duración:</strong> <%= movie.length %> minutos</p>
                <p class="card-text"><strong>Género:</strong> <a href="/genres/<%= movie.genre.id %>"><%= movie.genre.name %></a></p>
                
                <h5 class="mt-4">Actores</h5>
                <ul class="list-group mb-3">
                    <% for(let i=0; i<movie.actors.length; i++){ %>
                        <li class="list-group-item">
                            <a href="/actors/<%= movie.actors[i].id %>"><%= movie.actors[i].first_name + " " + movie.actors[i].last_name %></a>
                        </li>
                    <% } %>
                </ul>

                <form action="/movies/delete/<%= movie.id %>" method="POST" class="d-inline">
                    <button type="submit" class="btn btn-danger">Borrar</button>
                </form>
                <form action="/movies/editMovie/<%= movie.id %>" method="get" class="d-inline">
                    <button type="submit" class="btn btn-warning">Modificar</button>
                </form>                
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>

</html>
